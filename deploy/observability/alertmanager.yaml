global:
  resolve_timeout: 5m

route:
  receiver: gameservice-ops
  group_by: [alertname, service]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: gameservice-ops
    webhook_configs:
      - url: __ALERTMANAGER_WEBHOOK_URL__
        send_resolved: true
